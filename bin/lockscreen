#!/bin/bash

image=~/.config/i3/.tmp.png
lockicon=~/.config/i3/.lockicon.png

scrot $image

convert $image -scale 10% -modulate 100,50 -scale 1000% $lockicon -compose Over -gravity center -composite $image

i3lock -e -i $image -u
